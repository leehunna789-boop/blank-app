
import os

# ==========================================================
# 📋 ส่วนที่ 1: "กระดานเขียนเพลง" (คุณจัดการแค่ตรงนี้)
# ==========================================================

# 1. อยากเปลี่ยนเพลงอื่น ก็แค่มาเปลี่ยนชื่อไฟล์ตรงนี้
FILE_PATH = "my_new_song.mp3" 

# 2. อยากเปลี่ยนเนื้อเพลงอื่น ก็ลบของเก่าแล้วเขียนใหม่ในนี้
# (วินาทีที่ต้องการให้ขึ้น : "เนื้อเพลงที่อยากให้โชว์")
MY_BOARD = {
    5:   "--- [เริ่มเพลงใหม่] ---",
    30:  "ท่อนฮุคเพลงใหม่มาแล้ว...",
    180: "--- [จบเพลงภายใน 3 นาที] ---"
}

# ==========================================================
# ⚙️ ส่วนที่ 2: "ไส้ในเครื่องยนต์" (รันเส้นทางมิติเดิมตลอด)
# ==========================================================

def start_vocal_engine():
    start_time = time.time()
    
    # ด่านตัดสินใจ (IF): เช็คหลักฐานไฟล์เสียง
    if os.path.exists(FILE_PATH):
        print(f"✅ ตรวจพบมิติเสียง: {FILE_PATH}")
        os.startfile(FILE_PATH) # สั่งเครื่องเสียงทำงาน
    else:
        print("❌ ไม่พบไฟล์เสียง (แต่กระดานเนื้อเพลงจะยังรันต่อ)")

    while True:
        # ด่านคำนวณความต่าง (DIFF): คุมเวลาให้ไม่คลาดเคลื่อน
        diff = time.time() - start_time
        current_sec = int(diff)

        # เช็คบนกระดานว่าถึงวินาทีที่ตั้งไว้หรือยัง
        if current_sec in MY_BOARD:
            print(f"🎵 [{current_sec}s] >> {MY_BOARD[current_sec]}")
            del MY_BOARD[current_sec] # ร้องแล้วลบทิ้ง กันรันซ้ำ

        # ด่านตัดจบที่ 3 นาที (180 วินาที)
        if diff >= 180:
            print("🏁 จบการรันเพลงบนกระดานนี้")
            break
            
        time.sleep(0.5) # ป้องกันเครื่องประมวลผลจนร้อนเกินไป

# สั่งให้ระบบ "ไปเองได้เลย"
if __name__ == "__main__":
    start_vocal_engine()
